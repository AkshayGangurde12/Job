// types.js (optional in JS projects)

// This is only kept if you actually need these constants at runtime.
// Otherwise you can delete this file entirely.

export const Constants = {
  public: {
    Enums: {},
  },
};
